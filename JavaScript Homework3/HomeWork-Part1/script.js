//console.log("Connected");

function getUserInput(x) {
    let userInput = x;
    // Determening the type of the input from the user with if / else if statements
    if(userInput === Infinity) {
        console.log(typeof userInput);
    } else if(userInput === true) {
        console.log(typeof userInput);
    } else if(userInput === " "){
        console.log(typeof userInput);
    } else {
        console.log(typeof userInput);
    }
}

// Calling the function
getUserInput();




